import 'package:flutter/material.dart';

class Topics45Tab extends StatelessWidget {
  const Topics45Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Themen 4.–5. Klasse'),
    );
  }
}
