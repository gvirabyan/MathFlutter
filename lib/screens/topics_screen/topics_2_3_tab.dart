import 'package:flutter/material.dart';

class Topics23Tab extends StatelessWidget {
  const Topics23Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Themen 2.–3. Klasse'),
    );
  }
}
