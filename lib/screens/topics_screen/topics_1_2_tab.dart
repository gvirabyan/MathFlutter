import 'package:flutter/material.dart';

class Topics12Tab extends StatelessWidget {
  const Topics12Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Themen 1.–2. Klasse'),
    );
  }
}
