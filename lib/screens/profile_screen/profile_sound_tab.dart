import 'package:flutter/material.dart';

class ProfileSoundTab extends StatelessWidget {
  const ProfileSoundTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ton', style: TextStyle(fontSize: 18)),
    );
  }
}
