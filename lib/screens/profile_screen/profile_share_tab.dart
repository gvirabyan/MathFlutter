import 'package:flutter/material.dart';

class ProfileShareTab extends StatelessWidget {
  const ProfileShareTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Erfolg teilen', style: TextStyle(fontSize: 18)),
    );
  }
}
